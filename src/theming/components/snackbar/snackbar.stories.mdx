import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import * as stories from './snackbar.stories';

<Meta
  title="Overrides/Snackbar"
  parameters={{
    viewMode: 'canvas',
    options: { showPanel: false }
  }}
/>

<Canvas>
  <Story name="Snackbar" story={stories.Demo} />
</Canvas>
